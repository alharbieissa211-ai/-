rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // بيانات المحفظة: القراءة متاحة للجميع (لوحة عامة)، والكتابة فقط لمستخدم مسجّل دخوله.
    match /portfolio/{docId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
